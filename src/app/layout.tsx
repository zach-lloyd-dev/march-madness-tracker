import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "March Madness Tracker 2026 | NCAA Tournament Schedule & Scores",
  description:
    "Clean, auto-updating NCAA March Madness 2026 tournament schedule. See today's games, tip times, TV channels, streaming platforms, and live scores.",
  openGraph: {
    title: "March Madness Tracker 2026",
    description: "Clean NCAA tournament schedule with scores, TV channels & streaming info",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
